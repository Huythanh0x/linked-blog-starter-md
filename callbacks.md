> A **function passed as an argument** to another function, to be **called later**, typically after an async operation.

Callbacks let you handle results **after** async tasks finish, without pausing the main thread.